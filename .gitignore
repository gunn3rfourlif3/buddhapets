node_modules/
.next/
out/
# Anchored to the repo root on purpose: an unanchored `build/` also matches
# app routes such as apps/web/app/rituals/build, silently dropping them.
/build/
.env
.env.local
*.log
.DS_Store

# WordPress lives in its own install; only wp-content is version-controlled
buddhapets/
wp/wp-admin/
wp/wp-includes/
